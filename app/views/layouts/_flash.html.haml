- unless flash.blank?
  .container
    .row
      .col-sm-12
        #flash
          - flash.each do |key, value|
            %div{:class => "flash alert alert-dismissible fade in alert-#{key == 'notice' ? 'info' : 'danger'}"}
              %button.close{"aria-label" => "Close", "data-dismiss" => "alert", :type => "button"}
                %span{"aria-hidden" => "true"} ×
              = value
